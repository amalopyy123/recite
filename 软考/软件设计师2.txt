有三种类型的EJB,分别是会话Bean（Session Beans）、实体Bean（Entity Beans）和消息驱
动Bean（Message-driven Beans）。

动态规划和分治法的区别：动态规划保存了子问题的答案，避免了重复计算
动态规划：状态转移方程，BFS（Breath First Search）DP（Dynamic programming）

信息安全有5个基本要素，分别为机密性、完整性、可用性、可控性和可审查性。
机密性是指确保信息不暴露给未授权的实体或进程；
完整性是指只有得到允许的人才能够修改数据，并能够判别数据是否已被篡改；
可用性是指得到授权的实体在需要时可访问数据；
可控性是指可以控制授权范围内的信息流向和行为方式；
可审查性是指对出现的安全问题提供调查的依据和手段。


RAM,ROM,SRAM,DRAM,SSRAM（Synchronous SRAM）,SDR

主存主要采用动态随机存储器DRAM，Cache采用静态随机存储器SRAM

表格法：取行号小的b，后续也要参与替换；后面的替换还要考虑前面替换的条件。


数据流图的存储文件，后面要带“表”，“文件等后缀”
数据流图的存储文件不一定有两个箭头
学生 信息（表）
路线 信息 （表）
物流商 信息 （表）
问组成：
学生 编号
家长 编号
编号可能要自己加


数据流的回答包括起点终点和名称，不可以少了名称


图中某个加工的输入输出数据流必须与其子图的输入输出数据流在数量上和内容上保持一致，即数据不会凭空产生，也不能凭空消失。
父图的一个输入（或输出）数据流应对应子图中几个输入（或输出）数据流，而子图中组成的这些数据流的数据项全体正好是父图中的这个数据流。


ER图的联系，可能有三条或以上的连线



分治策略：递归

外模式，概念模式，内模式


两级映射分别是：外模式与概念模式之间的映射、概念模式与内模式之间的映射。
外模式与概念模式之间的映射：用于维护数据库的逻辑独立性。也就是说，有了这个映射，使得数据的逻辑结构改变时，应用程序不需要改变，只需要改变映射中的对应关系即可达到目的。
概念模式与内模式之间的映射：用于维护数据库的物理独立性。也就是说当数据的物理存储改变时，应用程序不需要改变。


进程执行前进行P操作，执行后进行V操作，这个V操作可能是V后面的进程的信号量（初始信号量p可能为0）

软件工程是一种层次化的技术，从底向上分别为质量、过程、方法和工具。

要使加速比和效率最大化：流水线各级采用相同的运行时间

 高级语言不依赖于具体的机器硬件

简单选择排序：外层循环游标为i，内层循环每次和a[i]比较大小决定是否换位，是不稳定的排序。
直接插入排序是稳定的排序
快速（两端交换）排序：设定一个分界值，将大于分界值的集中到右边，小于分界值的集中到左边

归并排序最坏和最好的情况下的时间复杂度都是O(nlgn)

VLAN(Virtual Local Area Network)：虚拟局域网，使设备不再局限于物理位置的约束，无论主机在哪里，它都处于自己的VLAN中。VLAN之间不能直接通信，必须经过特殊的路由才可以连通。在大型网中创建不同的VLAN，可以抵御广播风暴的影响。VLAN不能直接增强网络的安全性。

优先队列通常用堆来实现，获取最大元素的时间复杂度是O(1)，插入元素或增大某个元素的值的时间复杂度为O(lgn)。

敏捷开发：
4大价值观：沟通，简单，反馈，勇气
5大原则：快速反馈，简单性假设，逐步修改，提倡更改，优质工作
（faaak）

DMZ是指非军事化区，也称周边网络，可以位于防火墙之外也可以位于防火墙之内。非军事化区一般用来放置提供公共网络服务的设备，这些设备由于必须被公共网络访问，所以无法提供与内部网络主机相等的安全性。

传输媒体，表示媒体，表现媒体，存储媒体

进程资源图：
先看资源数量，再看分配出去的，最后看申请的


多态分为4种不同的形式：参数多态、包含多态、过载多态和强制多态。其中参数多态是应用比较广的多态，包含多态在许多语言中都存在，最常见的例子就是子类型化。过载多态是同一个名字在不同的上下文中所代表的含义。


软件评审的内容包括设计质量评审、程序质量评审和与运行环境接口的评审。评审的主要目标是为了发现软件中的错误。

UML中有4种关系：依赖、关联、泛化和实现。
依赖（Dependency)是两个事物间的语义关系，其中一个事物（独立事物）发生变化会影响另一个事物（依赖事物）的语义。
关联（Association)是一种结构关系，它描述了一组链，链是对象之间的连接。
泛化（Generalization)是一种特殊/一般关系，特殊元素（子元素）的对象可替代一般元素（父元素）的对象，用这种方法，子元素共享了父元素的结构和行为。
实现（Realization)是类元之间的语义关系，其中一个类元指定了由另一个类元保证执行的契约。

___开发过程模型以用户需求为动力，以对象为驱动，适合于面向对象的开发方法。
A.瀑布                                   B.原型                        C.螺旋                        D.喷泉
D

统一过程（UP）的基本特性是以用例和风险驱动，以架构为中心，受控迭代式的增量开发。
UP定义了四个阶段：
起始阶段
精化阶段
构建阶段
移交阶段

起始（初始）阶段的里程碑是生命周期目标
精化周期的里程碑是生命周期架构
构建周期的目标是初始运作功能
移交阶段的目标是产品发布



一个对象通常可由对象名、属性和方法三部分组成


数前面有几个元素：分特殊行和一般行

基本有序应该的选择 插入排序，取值范围小的选择计数排序


RUP（Rational Unified Process），统一软件开发过程，统一软件过程是一个面向对象且基于网络的程序开发方法论。

RUP应用了角色、活动、制品和工作流4种重要的模型元素，其中角色表述“谁做”，制品表述“做什么”，活动表述“怎么做”，工作流表述“什么时候做”。

RUP采用迭代和增量的方式来开发软件


假设磁头当前位于第99道，正在向磁道序号增加的方向移动。现有一个磁道访问请求序列为33，59，13，77，123，170，160，185，
采用SCAN调度(电梯调度)算法得到的磁道访问序列是( )
123，160，170，185，77，59，33，13

软件能力成熟模型（capability maturity model,CMM）分为五个等级
初始级
可重复级
已定义级
已管理级
优化级


敏捷方法中，重构是一种重新组织技术，重新审视需求和设计，重新明确地描述它们以符合新的和现有的需求，可以简化构件的设计而无需改变其功能或行为。


访问者模式表示一个作用于某对象结构中的各元素的操作。它使在不改变各元素的 类的前提下可以定义作用于这些元素的新操作。


堆排序：构造初始堆要从最后一个非叶节点开始调整

操作系统的功能可分为：处理机管理，文件管理，存储管理，设备管理，作业管理

缺页中断和一般中断的区别


处理机管理实质上是对处理机的执行“时间”进行管理，采用多管道技术将CPU的时间合理地分配给每个任务，只要包括进程管理，进程同步，进程通信和进程调度

桩模块：代替其他模块的功能
驱动模块：代替main函数
驱动模块调用被被模块，被测模块调用桩模块

结构化分析方法由以下几部分组成：
分层数据流图
数据词典
加工逻辑说明（小说明）
补充材料

数据库：规范化程度越高，数据的密度越小
要把范式提高级别，基本上都要进行数据表的拆分

