泛化：实线空三角
实现：虚线空三角
交互图：序列图（跟顺序图一样最上方有桩），通信图，交互预览图，时序图（很像示波器，新增的）
活动图：并发分岔，并发汇合，监护表达式，流，动作
迭代器模式（Iterator），提供一种方法顺序访问一个聚合对象中的各种元素，而又不暴露该对象的内部表示。
解释器模式（Interpreter Pattern）给定一个语言，定义它的文法表示，并定义一个解释器，这个解释器使用该标识来解释语言中的句子。
物理层，数据链路层=》网络接口层
关系模式的除运算：根据除数选出合适的行，去掉重复行，去掉除数的列
左外连接：右边出现null
右外连接：左边出现null
全外连接：两边出现null
视图是虚拟表，其内容由查询的来决定
逻辑地址在整个互联网有效，物理地址在子网内部有效，在Internet中应用程序把报文交给IP协议，要知道接收方的逻辑地址，但不一定要知道接收方的物理地址
专利权的保护期限：自申请日起20年
外观设计的保护期限：自申请日起10年
软件著作权的保护期限：自完成之日起50年
单位作品的保护期限也是50年，但是需要发表
无论是否发表都享有著作权
软件作品的委托开发，有合同的参照合同，没合同的给创作方
商标权和专利权谁先申请给谁
数据库设计的四个主要阶段：需求分析，概念结构设计，逻辑结构设计，物理结构设计
有序（聚簇）存档
非对称加密（公开密钥加密算法）：
ECC，DSA，RSA
对称加密算法（共享密钥加密算法）：
DES，3DES，AES，IDEA，RC4，RC5
RC5是对称加密算法，比非对称加密算法效率要高，适合加密大量数据
词法分析器的任务是把源文件的字符串流转换成记号流；
语法分析器根据语法规则识别出记号流中的结构（短语，句子），并构造一棵能够正确反映该结构的语法树；语法分析主要检查句子的结构是否符合语法规则。
”程序中的括号不匹配“属于语法错误。
磁盘调度：先移臂，再旋转
质量特性：
功能性
效率
易用性
维护性
可靠性
可移植性
开放封闭是指类要对扩展开放，对修改关闭
索引属于内模式
A→BC等价于A→B并且A→C
程序计数器取出来的是指令，指令地址码部分取出来
是数据，CPU通过不同的寻址方式来区别指令和数据
SNMP在是应用层的协议，使用UDP封装和传送
动态错误发生在语句执行的时候
命令模式：将一个请求封装成一个对象，从而可以用不同的请求对客户进行参数化
对请求排队或记录请求日志，以及支持可撤销的操作。
责任链模式：使多个对象都有机会处理请求，从而避免请求的发送者和接收者之间耦合关系。将这些对象连成一条链，并沿着这条链传递该请求，直到有一个对象处理它为止。
观察者模式：定义对象间的一种一对多的依赖关系，当一个对象的状态发生变化时，所有依赖于它的对象都得到通知并被自动更新。
策略模式：定义一系列的算法，把每一个算法封装起来，并且它们可以互相替换。本模式使得算法可以独立于使用它的客户的变化。
工厂方法模式：也叫做虚拟构造器模式，它定义一个用于创建对象的接口，让子类决定实例化哪一个类，使一个类的实例化延迟到其子类。
原型模式：用原型实例指定创建对象的种类，并且复制这个原型创建新的对象。
单例模式：确保某一个类只有一个实例，并且自动实例化并向整个系统提供这个实例，这个类称为单例类，它提供全局访问的方法。
抽象工厂模式定义：提供一个创建一系列相关或者相互依赖对象的接口，而无需知道他们的具体类，抽象工厂模式也称Kit模式，它属于类创建型模式。
在抽象工厂模式中，每个具体工厂都提供了多个工厂方法用于创建多种不同类型的具体对象，这些被创建的对象就构成一个族
生成器模式：把对象的创建步骤抽象成生成器，并且可以通过指导类（director）对所有生成步骤的先后顺序进行控制
代理模式：为其它对象提供一种代理以控制这个对象的访问，例如快捷方式
主要用于不适合或者不能直接引用另一个对象的场景
被动攻击是指收集信息而不是进行访问，数据的合法用户对这些活动一点都不会觉察到。系统干涉属于被动攻击。
专家系统，模型检测，漏洞扫描属于主动攻击。
漏洞扫描是网络安全技术，属于安全检测技术，不属于入侵检测技术。
功能需求和性能需求
时钟周期：最小的时间单位
执行指令的过程分为：取指令，分析指令和执行指令
指令周期由若干个机器周期组成
挂在总线上的部件，只能分时向总线发送数据，但可同时从总线接收数据
cache对程序员透明
抽象工厂：不同的工厂生产不同的产品类型
具体工厂：不同的工厂生产同一类型的产品
求候选关键字：遍历全图
数字签名算法：非对称加密，而不是信息摘要
MAC地址：物理地址，硬件地址
中继器：基于烽火台机制原理，对信号放大再生，以延长网络传输距离